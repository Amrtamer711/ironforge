# =============================================================================
# Video Critique Service - Production Dependencies (Render)
# =============================================================================
# This file is used for Render deployments where editable installs don't work.
# Configure Render build command: pip install -r requirements.render.txt

# Core dependencies
slack-sdk==3.36.0
fastapi==0.116.1
uvicorn[standard]==0.30.6
python-multipart==0.0.20

# Data processing
pandas>=2.1.0,<2.3.0
openpyxl==3.1.5
numpy>=2.0.0,<3.0.0

# OpenAI (let crm-llm handle version, requires >=2.8.1)
# openai version managed by crm-llm SDK

# Dropbox
dropbox==12.0.2

# Utils
python-dotenv==1.0.1
aiofiles==24.1.0
pydantic>=2.10.0
pydantic-settings>=2.1.0
Pillow==11.3.0
requests==2.32.3
pytz==2024.1
aiohttp==3.12.15
hijri-converter==2.3.2.post1
httpx==0.27.2

# Supabase (for database)
supabase>=2.11.0

# Redis (for distributed caching)
redis>=5.0.0

# JWT (for token handling)
python-jose[cryptography]>=3.3.0

# CRM Security SDK (git install for production)
# GitLab (switch to this when migrated): crm-security[fastapi] @ git+https://gitlab.com/mmg-global/ironforge.git@main#subdirectory=src/shared/crm-security
crm-security[fastapi] @ git+https://github.com/Amrtamer711/ironforge.git@dev#subdirectory=src/shared/crm-security

# CRM LLM SDK (git install for production)
# GitLab (switch to this when migrated): crm-llm[openai] @ git+https://gitlab.com/mmg-global/ironforge.git@main#subdirectory=src/shared/crm-llm
crm-llm[openai] @ git+https://github.com/Amrtamer711/ironforge.git@dev#subdirectory=src/shared/crm-llm

# CRM Channels SDK (git install for production)
# GitLab (switch to this when migrated): crm-channels @ git+https://gitlab.com/mmg-global/ironforge.git@main#subdirectory=src/shared/crm-channels
crm-channels @ git+https://github.com/Amrtamer711/ironforge.git@dev#subdirectory=src/shared/crm-channels

# CRM Cache SDK (git install for production)
# GitLab (switch to this when migrated): crm-cache[redis] @ git+https://gitlab.com/mmg-global/ironforge.git@main#subdirectory=src/shared/crm-cache
crm-cache[redis] @ git+https://github.com/Amrtamer711/ironforge.git@dev#subdirectory=src/shared/crm-cache
