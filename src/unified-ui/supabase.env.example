# Unified UI runtime env (Kubernetes/Render)
#
# Copy to `src/unified-ui/supabase.env` (gitignored) and fill values from Render (or your secrets store).
#
# Minimum required for login/signup:
# - UI_DEV_SUPABASE_URL
# - UI_DEV_SUPABASE_ANON_KEY
# - UI_DEV_SUPABASE_SERVICE_ROLE_KEY
#
# Optional (fallback names used by the codebase):
# - SUPABASE_URL
# - SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_KEY

ENVIRONMENT=development
PORT=3005

# Supabase (development)
UI_DEV_SUPABASE_URL=https://your-project.supabase.co
UI_DEV_SUPABASE_ANON_KEY=
UI_DEV_SUPABASE_SERVICE_ROLE_KEY=

# Supabase (production)
UI_PROD_SUPABASE_URL=
UI_PROD_SUPABASE_ANON_KEY=
UI_PROD_SUPABASE_SERVICE_ROLE_KEY=

# Fallback keys (optional)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# Backend service URLs (optional; dev overlay sets SALES_MODULE_URL explicitly)
SALES_MODULE_URL=
ASSET_MGMT_URL=
SECURITY_SERVICE_URL=

# Shared secret for proxy auth (recommended)
PROXY_SECRET=
