# =============================================================================
# Unified UI - Production Dependencies (Render)
# =============================================================================
# This file is used for Render deployments where editable installs don't work.
# Configure Render build command: pip install -r requirements.render.txt

# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# HTTP Client (for proxy requests)
httpx>=0.26.0

# Supabase
supabase>=2.3.0

# Pydantic (settings and models)
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# JWT (for token decoding/debugging)
python-jose[cryptography]>=3.3.0
PyJWT>=2.8.0

# CRM Security SDK (git install for production)
crm-security[fastapi] @ git+https://gitlab.com/mmg-global/ironforge.git@main#subdirectory=src/shared/crm-security

# YAML parsing (for dev panel, minimal in production)
PyYAML>=6.0
